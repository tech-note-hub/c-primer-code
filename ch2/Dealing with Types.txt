2.5. Dealing with Types

vector<string>::iterator

Type Aliases, auto နဲ့ decltype

1.Type Aliases
a.typedef

typedef double wages;

2.using

using SI = Sales_item;
using count = int;

typedef char *pstring;
const pstring cstr = 0;


2.The auto Type Specifier
auto item = val1 + val2; // compiler decides the type

const int ci = i; auto b = ci
const auto b = ci;


The decltype Type Specifier

decltype(f()) sum = x; // sum has the return type of f()

decltype(i)
decltype((i))